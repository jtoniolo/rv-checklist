apiVersion: v1
kind: Service
metadata:
  name: {{ include "rv-checklist-api.fullname" . }}
  labels:
    {{- include "rv-checklist-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rv-checklist-api.selectorLabels" . | nindent 4 }}
