apiVersion: v2
name: rv-checklist-api
description: Helm chart for the RV Checklist API service
type: application
# A release is a git tag. Bump both of these in the same commit, then tag
# `vX.Y.Z` on it — CD refuses to publish when the tag and these disagree.
# `appVersion` is what the Deployment resolves its image tag from.
version: 0.2.7
appVersion: "0.2.7"
