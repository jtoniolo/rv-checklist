apiVersion: v1
kind: Service
metadata:
  name: {{ include "rv-checklist-web.fullname" . }}
  labels:
    {{- include "rv-checklist-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rv-checklist-web.selectorLabels" . | nindent 4 }}
